import TopAppBar from "../components/top-app-bar";

export default function Message() {
    return (
        <div className="relative w-full h-full bg-teal-50">
            <TopAppBar />
        </div>
    );
}